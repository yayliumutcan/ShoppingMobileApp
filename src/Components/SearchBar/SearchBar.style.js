import { StyleSheet } from "react-native";

export default StyleSheet.create({

    container: {
        flexDirection: 'row',
        margin: 10,
        backgroundColor: 'white',
        borderColor: '#7986cb',
        borderBottomWidth: 3,
        borderRightWidth: 2,
        borderRadius: 10,
    },

    input: {
        color: 'black',
        fontSize: 15,
        height: 40,
    },
});